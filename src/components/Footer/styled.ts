import styled from 'styled-components';

export const StyledFooter = styled.footer`
  display: grid;
  height: 100%;
  background-color: #705d84;
  grid-area: footer;

  section {
    place-self: end center;
    padding: 16px;
  }
`
