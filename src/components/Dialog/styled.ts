import styled from "styled-components";

export const StyledDialog = styled.dialog`
  position: absolute;
  top: 0;
  padding: 24px 24px 8px;
  color: black;
  background: white;

  p {
    padding: 16px;
  }

  button {
    color: white;
    margin: 16px 8px;
  }
`
