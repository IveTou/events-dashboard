import styled from "styled-components";

export const StyledDetails = styled.article`
  padding: 8px 24px;

  h2 {
    background: lavender;
    padding: 8px 16px;
  }

  section {
    background: #e6e6e6;
    padding: 8px 16px;

    div {
      display: flex;
      gap: 16px;
      margin-bottom: 16px;
    }
  }
`
